# pv-prospect-etl
ETL components for PV Prospect projects. This library houses shared storage protocols and implementations (e.g. `LocalClient`, `GcsClient`) along with specialized `loaders` to write data and `extractors` to read data.
